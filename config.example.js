// ============================================================
// CONFIG TEMPLATE — copy this file to config.js and fill in your Vercel URL
// The API key is stored in Vercel environment variables, not here
// ============================================================

const CONFIG = {
  PROXY_URL: "https://your-project.vercel.app/api/check",
};
